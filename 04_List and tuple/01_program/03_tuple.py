# tuple are immutable like string..we cannot change the data
# in tuple data are store in () formate

t1 = (1, 2.3, "sourabh", False)
print(t1)
t2 = (1,)  # only one single item and empty tuple is ()

print(type(t1))